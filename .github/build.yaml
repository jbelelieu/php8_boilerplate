name: pipeline
on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master
jobs:
  # test:
  #   name: test
  #   runs-on: ubuntu-latest
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: michaelw90/PHP-Lint@master
